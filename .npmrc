; npm 12 disables git dependencies by default (allow-git=none). @chainlink/contracts
; pulls @zksync/contracts from github, so npm ci fails with EALLOWGIT without this.
; "root" does not help: it only covers git URLs this package declares directly.
allow-git=all
